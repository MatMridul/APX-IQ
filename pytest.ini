[pytest]
testpaths = tests test_intelligence_audit.py
norecursedirs = ui node_modules .git .venv graphify-out data
asyncio_mode = strict
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
