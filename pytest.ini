[pytest]
testpaths = tests
norecursedirs = ui node_modules .git .venv graphify-out data
asyncio_mode = strict
markers =
    integration: requires live PostgreSQL via DATABASE_URL (run separately)
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
