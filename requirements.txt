# ─── Core Ingestion ───────────────────────────────────────────────────────────
python-socketio
aiohttp

# ─── API Layer ────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0    # Centralized settings via core/config.py
httpx>=0.27.0
asyncpg>=0.29.0
python-dotenv>=1.0.0

# ─── Rate Limiting & Observability ───────────────────────────────────────────
slowapi>=0.1.9
structlog>=24.0.0

# ─── Intelligence Layer ───────────────────────────────────────────────────────
fastf1>=3.4.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
# scikit-learn removed — was imported but never used

# ─── GenAI (Optional) ─────────────────────────────────────────────────────────
# Falls back to local Ollama → deterministic template if not installed
google-genai>=1.0.0
